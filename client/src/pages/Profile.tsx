import { SiteLayout } from "@/components/SiteLayout";
import { Button } from "@/components/ui/button";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { ExternalLink, Link2, Settings2, ShieldCheck, UserRound } from "lucide-react";
import { Link } from "wouter";

export default function Profile() {
  const { user, isAuthenticated } = useAuth();
  const { data: profile } = trpc.profile.me.useQuery(undefined, { enabled: isAuthenticated });
  const active = profile || user;
  if (!isAuthenticated || !active) return <SiteLayout><div className="container max-w-2xl py-20 text-center"><UserRound className="mx-auto size-10 text-gold" /><h1 className="mt-5 font-display text-5xl uppercase text-white">Your profile</h1><p className="mt-4 text-sm text-muted-foreground">Sign in to create and manage your public SGTB profile.</p><Button onClick={() => startLogin()} className="mt-7 bg-gold text-black hover:bg-gold-soft">Sign in</Button></div></SiteLayout>;
  let links: Array<{ label: string; url: string }> = [];
  try { links = active.socialLinksJson ? JSON.parse(active.socialLinksJson) : []; } catch { links = []; }
  return <SiteLayout><div className="container max-w-5xl py-10 sm:py-16"><div className="overflow-hidden rounded-[2rem] border border-gold/20 bg-[radial-gradient(circle_at_top_right,rgba(244,191,55,0.2),transparent_40%),#0b0b0e] shadow-2xl"><div className="h-32 bg-gradient-to-r from-gold/20 via-neon/10 to-transparent sm:h-44" /><div className="-mt-12 flex flex-col gap-5 px-6 pb-8 sm:-mt-16 sm:flex-row sm:items-end sm:px-10"><div className="grid size-28 shrink-0 place-items-center overflow-hidden rounded-[2rem] border-4 border-[#0b0b0e] bg-gold text-3xl font-bold text-black shadow-[0_0_35px_rgba(244,191,55,0.25)] sm:size-36">{active.avatarUrl ? <img src={active.avatarUrl} alt={`${active.name || "SGTB"} avatar`} className="size-full object-cover" /> : (active.name || active.email || "SG").slice(0, 2).toUpperCase()}</div><div className="min-w-0 flex-1"><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-neon">{active.role === "admin" ? "Owner Admin" : active.role === "rep" ? "Suno Representative" : "SGTB Network Member"}</p><h1 className="mt-2 truncate font-display text-4xl uppercase leading-none text-white sm:text-6xl">{active.name || "SGTB Member"}</h1><p className="mt-2 text-sm text-muted-foreground">{active.username ? `@${active.username}` : active.email}</p></div><Link href="/settings"><Button variant="outline" className="border-gold/30 text-gold hover:bg-gold/10"><Settings2 className="mr-2 size-4" /> Edit profile</Button></Link></div><div className="grid gap-6 border-t border-white/10 px-6 py-7 sm:grid-cols-[minmax(0,1fr)_260px] sm:px-10"><div><p className="whitespace-pre-wrap text-sm leading-7 text-white/75">{active.bio || "This member has not added a bio yet. Share your signal and build your public profile from Account Settings."}</p><div className="mt-5 flex flex-wrap gap-2">{links.map((link) => <a key={`${link.label}-${link.url}`} href={link.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-white/10 px-3 py-2 text-xs text-muted-foreground hover:border-gold/30 hover:text-gold"><Link2 className="size-3.5" /> {link.label} <ExternalLink className="size-3" /></a>)}{active.websiteUrl && <a href={active.websiteUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-gold/20 bg-gold/5 px-3 py-2 text-xs text-gold"><ExternalLink className="size-3.5" /> Website</a>}</div></div><div className="rounded-2xl border border-white/10 bg-black/20 p-5"><p className="font-mono text-[10px] uppercase tracking-wider text-gold">Clearance</p><p className="mt-3 flex items-center gap-2 font-display text-xl uppercase text-white">{active.role === "admin" || active.role === "rep" ? <ShieldCheck className="size-5 text-gold" /> : <UserRound className="size-5 text-muted-foreground" />}{active.role}</p><p className="mt-2 text-xs leading-6 text-muted-foreground">Profile identity is secured by Manus OAuth. Account email credentials remain managed by the identity provider.</p></div></div></div></div></SiteLayout>;
}
