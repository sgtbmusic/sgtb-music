import { SiteLayout } from "@/components/SiteLayout";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { useAudioPlayer } from "@/contexts/AudioPlayerContext";
import { ArrowUpRight, AudioLines, Compass, Film, Music2, Pause, Play, Sparkles } from "lucide-react";
import { Link } from "wouter";

export default function Explore() {
  const { data: posts, isLoading } = trpc.social.feed.useQuery();
  const audio = useAudioPlayer();
  return <SiteLayout><div className="container max-w-6xl py-10 sm:py-16"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.28em] text-neon"><Compass className="size-3" /> Discover the signal</p><h1 className="mt-3 font-display text-6xl uppercase leading-[0.82] text-white">Explore <span className="text-gold-gradient">SGTB.</span></h1><p className="mt-5 max-w-2xl text-sm leading-7 text-muted-foreground">Discover community signals, Reference Demos, visual DNA, and executive-ready production pathways.</p></div><Link href="/"><Button className="bg-gold text-black hover:bg-gold-soft">Open main feed <ArrowUpRight className="ml-2 size-4" /></Button></Link></div><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><Link href="/music" className="group rounded-3xl border border-gold/20 bg-gold/5 p-5 transition hover:-translate-y-1 hover:bg-gold/10"><Music2 className="size-6 text-gold" /><h2 className="mt-8 font-display text-2xl uppercase text-white">Catalog</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Stream curated master tracks and Reference Demos.</p></Link><Link href="/visuals" className="group rounded-3xl border border-neon/20 bg-neon/5 p-5 transition hover:-translate-y-1 hover:bg-neon/10"><Film className="size-6 text-neon" /><h2 className="mt-8 font-display text-2xl uppercase text-white">Visual DNA</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Review the retained SGTB car-and-chain sequence.</p></Link><Link href="/suno" className="group rounded-3xl border border-purple-300/20 bg-purple-300/5 p-5 transition hover:-translate-y-1 hover:bg-purple-300/10"><AudioLines className="size-6 text-purple-200" /><h2 className="mt-8 font-display text-2xl uppercase text-white">Suno Business</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Hear the A&R engine and studio pipeline.</p></Link><Link href="/artist-draft-pool" className="group rounded-3xl border border-white/10 bg-white/5 p-5 transition hover:-translate-y-1 hover:bg-white/10"><Sparkles className="size-6 text-gold" /><h2 className="mt-8 font-display text-2xl uppercase text-white">Draft Pool</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Review the financial-market style catalog exchange.</p></Link></div><section className="mt-12"><div className="flex items-center justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-gold">Community pulse</p><h2 className="mt-2 font-display text-4xl uppercase text-white">Recent signals</h2></div><Link href="/"><Button variant="outline" className="border-white/15 text-muted-foreground hover:text-white">View all</Button></Link></div>{isLoading ? <p className="mt-6 text-sm text-muted-foreground">Loading community signals…</p> : <div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{posts?.slice(0, 6).map(({ post, author }) => <article key={post.id} className="overflow-hidden rounded-3xl border border-white/10 bg-card/60"><div className="aspect-[4/3] bg-black/30">{post.mediaType === "image" && post.mediaUrl ? <img src={post.mediaUrl} alt={post.mediaTitle || "Community visual"} className="size-full object-cover" /> : post.mediaType === "audio" && post.mediaUrl ? <div className="grid size-full place-items-center p-5"><AudioLines className="size-10 text-gold" /><Button type="button" variant="outline" onClick={() => audio.toggleTrack({ id: post.id, title: post.mediaTitle || "Audio signal", artist: author?.name || "SGTB Member", audioUrl: post.mediaUrl!, kind: "feed" })} className="mt-5 border-gold/30 bg-gold/5 text-gold hover:bg-gold/10 hover:text-gold-soft">{audio.isCurrent({ id: post.id, kind: "feed" }) && audio.isPlaying ? <Pause className="mr-2 size-4" /> : <Play className="mr-2 size-4" />} {audio.isCurrent({ id: post.id, kind: "feed" }) && audio.isPlaying ? "Pause" : "Play"}</Button></div> : <div className="grid size-full place-items-center p-7 text-center"><Sparkles className="size-8 text-gold/70" /><p className="mt-3 line-clamp-4 text-sm leading-6 text-white/70">{post.body}</p></div>}</div><div className="p-4"><p className="font-display text-lg uppercase text-white">{author?.name || "SGTB Member"}</p><p className="mt-1 line-clamp-2 text-xs leading-5 text-muted-foreground">{post.body}</p><div className="mt-3 flex gap-3 font-mono text-[10px] uppercase tracking-wider text-muted-foreground"><span>♡ {post.likesCount}</span><span>Comments {post.commentsCount}</span></div></div></article>)}</div>}</section></div></SiteLayout>;
}
